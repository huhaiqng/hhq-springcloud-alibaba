server:
  port: 8081
  servlet:
    context-path: /product

spring:
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
  application:
    name: product
  cloud:
    nacos:
      config:
        file-extension: yaml
    discovery:
      server-addr: 127.0.0.1:8848