server:
  port: 8082
  servlet:
    context-path: /user

spring:
  application:
    name: user
  cloud:
    nacos:
      config:
        file-extension: yaml
    discovery:
      server-addr: 127.0.0.1:8848